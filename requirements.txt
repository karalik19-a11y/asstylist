# Flexible lower bounds so pip can pick prebuilt wheels for your Python version
# (exact pins broke source-builds on Python 3.14 / 32-bit Windows).
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
pydantic>=2.10.4
pydantic-settings>=2.7.0
python-multipart>=0.0.20
pillow>=11.0.0
httpx>=0.28.1
pytest>=8.3.4
pytest-asyncio>=0.25.0
